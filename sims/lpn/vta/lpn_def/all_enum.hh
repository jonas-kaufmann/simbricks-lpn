#ifndef __VTA_ALL_ENUM__
#define __VTA_ALL_ENUM__

enum class ALL_ENUM {
    INP,
    LOADUOP,
    GEMM,
    SYNC,
    WGT,
    LOADACC,
    ALU,
    COMPUTE,
    FINISH,
    EMPTY,
    STORE,
    LOAD
};
#endif
